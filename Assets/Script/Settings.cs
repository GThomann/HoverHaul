using UnityEngine;
using System.Collections;

public class Settings:MonoBehaviour {
    //atributos static (qualquer script tem acesso)
    public static float valMusVolume, valSfxVolume;
    public static int valDificuldade, numVoltas;

    // Use this for initialization
    void Start() {
        
    }
}
